---
php_fpm_pool_defaults:
  pm: dynamic
  pm.max_children: "{{ ansible_processor_vcpus * 2 }}"
  pm.start_servers: 2
  pm.min_spare_servers: 1
  pm.max_spare_servers: 2

php_fpm_default_version: "5.6"

php_fpm_secure_opcache: True

php_fpm_installed_versions: ['5.6', '7.0']

php_fpm_ini: []
